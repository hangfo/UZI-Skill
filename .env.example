# UZI-Skill · 本地环境变量（可选）
# 把本文件复制为 .env 并填入你自己的值：
#   cp .env.example .env
#   # 然后编辑 .env 填 key

# ─── 东财妙想 Skills Hub API（推荐配置）──────────────────────
# 设置后 UZI-Skill 会优先使用官方 NLP API 做：
#   1. 中文名纠错（"北部港湾" 自动识别为 "北部湾港"）
#   2. 股票行情快照（稳定性远高于爬 push2.eastmoney.com）
# 获取 key（免费）: https://dl.dfcfs.com/m/itc4
MX_APIKEY=

# ─── SEC EDGAR 自动访问（不要写入本文件）────────────────────
# evidence_overlay_builder 直接读取 Windows 用户环境中的 UZI_SEC_USER_AGENT，
# 不读取仓库 .env。请使用真实姓名/机构 + 可联系邮箱；缺失或占位身份会零请求失败：
# [Environment]::SetEnvironmentVariable(
#   "UZI_SEC_USER_AGENT",
#   "Your Real Name UZI-Skill/3.2 your.real.email@example.com",
#   "User"
# )
#
# CBOE：公开端点可达不等于获准使用。取得书面许可并在代码中加入许可门禁前，
# UZI 不配置 CBOE 变量、不发 CBOE 请求；当前 Yahoo options 仅作个人研究后备。

# ─── 其他可选 ─────────────────────────────────────────────
# STOCK_NO_CACHE=1        # 禁用所有 API 缓存，强制重新拉取
# UZI_NO_AUTO_OPEN=1      # 分析完不自动打开浏览器
